{
  "name": "knockout-diagram",
  "version": "0.1.0",
  "description": "A set of components to create diagrams and diagram editors using knockout",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Neil Herald",
  "license": "MIT",
  "devDependencies": {
    "babelify": "^6.1.3",
    "grunt": "^0.4.5",
    "grunt-browserify": "^3.8.0",
    "grunt-contrib-watch": "^0.6.1"
  }
}
